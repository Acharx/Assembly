       ;logic  operation instructions
       MOV     R0, #0xFF
       MOV     R1, #0xAA

       AND     R2, R0, R1
       ORR     R4, R0, R1
       EOR     R5, R0, R1
       ;lojik  işlemler bitwise şeklide yapılır
       ;yani   sayıların binary halleri bit bit kıyaslanır
